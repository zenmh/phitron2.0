#include <bits/stdc++.h>

using namespace std;

int main()
{
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n;
  cin >> n;

  int e = n - 1, am[n][n];

  while (e--)
  {
    int a, b;
    cin >> a >> b;
  }

  return 0;
}