#include <stdio.h>
#include <math.h>

int main()
{
  int a = ceil(4.1),     // 4
      b = floor(3.99),   // 3
      c = round(3.4999), // 3
      d = sqrt(16),      // 4
      e = pow(2, 3),     // 8
      f = abs(-10);      // 10

  return 0;
}
